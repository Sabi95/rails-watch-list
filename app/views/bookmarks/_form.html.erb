<%= simple_form_for [list, bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, prompt: "Choose a Movie", collection: Movie.order(:title)%>
  <%= f.submit class: 'btn btn-info mt-2' %>
<% end %>
